export const initWpMusicGalleries = () => {
  /**
   * @TODO
   * 1. Load Swiper.
   * 2. Configure Swiper.
   * 3. Attach music.
   * 4. Attach animations.
   */

  const galleriesToParse = Array.from(document.querySelectorAll('.wp-music-gallery'));

  console.log({
    galleriesToParse,
  });
};
